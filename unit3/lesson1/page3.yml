# yaml-language-server: $schema=https://vscodeedu.com/assets/schema/course-v2.schema.json
id: page3
title: The Agent in Action
content: >
  ## What Happened?


  You saw a tool card appear in the chat when you asked a question about the weather.
  The AI called the tool, got the information, and responded. Compare that to Lab 1,
  where it could only respond from training data.


  That is the difference between generative and agentic behavior. Same AI, same
  question, but now it has a tool and it used it.
questionGroups:
  - id: "1"
    content: Think about what you observed in the chat.
    questions:
      - id: "1"
        type: choice
        content: Did you have to tell the AI which tool to use, or did it figure that
          out on its own?
        options:
          - id: a
            value: I had to tell it
            incorrectExplanation: Even if you mentioned the weather in your question, you
              didn't instruct it to call a specific tool. It matched your
              question to the right tool on its own.
          - id: b
            value: It figured it out on its own
            explanation: Exactly. The AI read your message, determined the weather tool
              was relevant, and called it. You did not have to specify which
              tool to use. That autonomy is what makes it an agent.
            isCorrect: true
  - id: "2"
    questions:
      - id: "1"
        type: choice
        content: The agent retrieved live weather data, added it to the conversation,
          and then generated a response using that data. This pattern has a name
          in the industry. What is it?
        options:
          - id: a
            value: RAG (Retrieval-Augmented Generation)
            explanation: RAG is the pattern of retrieving external information, adding
              it to the AI's context, and generating a response with it. You
              just watched it happen in real time.
            isCorrect: true
          - id: b
            value: MCP (Model Context Protocol)
            incorrectExplanation: MCP is a standard for connecting tools to AI models.
              The pattern of retrieve, add to context, then generate is called RAG.
          - id: c
            value: RLHF (Reinforcement Learning from Human Feedback)
            incorrectExplanation: RLHF is how models improve from human ratings. The
              pattern you just saw, retrieving data and using it to respond, is
              called RAG.
